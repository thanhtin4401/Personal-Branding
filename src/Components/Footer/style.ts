import { Row } from "antd";
import { styled } from "styled-components";

export const FooterWrapper = styled(Row)`
  height: 100px;
  background: #fff;
  font-size: 32px;
  background-image: linear-gradient(to right, white, #e2fdff, white);
`;

export const TextFooter = styled.div`
  font-size: 32px;
  font-weight: 700;
  color: #8c8c8c;
  line-height: 46.5px;
`;
